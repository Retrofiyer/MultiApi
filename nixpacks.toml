# Especifica las fases del proceso de construcción
[phases.setup]
nixPkgs = ['nodejs']  # Instala Node.js

[phases.install]
cmds = ['npm install']  # Instala las dependencias del proyecto

# Si no tienes una fase de construcción, puedes omitir esta sección.
# La dejo comentada por si acaso tienes algún paso de compilación en el futuro.
#[phases.build]
#cmds = ['npm run build']  # Construye la aplicación si es necesario

# Define cómo debe iniciarse la aplicación
[start]
cmd = 'npm run start'  # Comando para iniciar la aplicación

[project]
directory = '/'  # Asegúrate de que este directorio sea correcto